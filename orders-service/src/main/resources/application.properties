# Close the JPA session when the transaction ends, instead of holding it open for
# the whole HTTP request. Spring Boot's default (true) prints a warning on every
# startup and lets lazy associations load during JSON serialization, which hides
# N+1 query problems from you. Turning it off makes those problems visible in the
# service layer, where they belong.
spring.jpa.open-in-view=false
